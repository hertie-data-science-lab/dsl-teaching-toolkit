"""The Instructor Console's side of the engine: the operations it may ask for
(`registry`), the request it sends (`request`) and the outcome it reads back (`outcome`).

The shapes are the build contracts `dsl.request/1` and `dsl.outcome/1`;
`python -m dsl_course.schemas` exports them as JSON Schema for the console."""
