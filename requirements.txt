# PINNED, not floored. Every seeded workflow in every live course installs this file at
# run time, so a new release on PyPI reaches production everywhere the moment it is
# published - with no run in between to catch it. Bump these deliberately, the way
# `central.CENTRAL_REF` is promoted.
#
# CORE only: what every toolkit module needs to import and run. The autograder's runtime
# lives in requirements-autograde.txt, which only the jobs that grade install - keep an
# import of one of its packages out of every module here (`collect` shells out to them).
PyYAML==6.0.3
# zoneinfo (stdlib) reads the OS tz database; tzdata is the pure-Python fallback so the
# schedule's timezone handling works even on minimal runners without a system tzdata.
tzdata==2026.3
# The `.gitignore` pattern matcher behind `.releaseignore` (see dsl_course/releaseignore.py).
# `GitIgnoreSpec.check_file(...).include` is the three-state verdict the nested-file walk
# needs - excluded / re-included by a `!` / no opinion at all - which `match_file`'s bare
# bool cannot express. The nesting and walk-pruning layer is ours; only the per-pattern
# translation is here.
pathspec==0.12.1
# Signs the JWT client assertion that authenticates the mailer to Entra ID with a certificate
# instead of a client secret (dsl_course/mailer.py). 42 was the floor for the timezone-aware
# not_valid_after_utc the expiry warning reads; pinned like everything else here.
cryptography==50.0.1
