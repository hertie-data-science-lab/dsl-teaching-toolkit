# The file a student opens in their private grades-<handle> repo.
# Shape only - generated by grades.gradebook_entry / build_gradebooks / render_yaml.
# Faculty-internal `auto` / `manual` columns are deliberately redacted; the student
# sees the authoritative `final`, not the machine/manual split.
#
# NOTE: the key names below are all the student ever gets - there is no legend
# anywhere except the one-line README beside this file. `team_grade`, `adjustment`
# and `final` are unexplained. Worth deciding whether the README should define them.
student: alexbeispiel
assignments:
  assignment-1:
    final: A-
    comments: Good work on Q3.
  assignment-4-project:
    team: team-x
    team_grade: B+
    adjustment: '+2'
    team_comments: Solid report; weak lit review.
    final: A-
    comments: Strong individual contribution.
