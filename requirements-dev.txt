# Tests need the autograder's runtime as well as the core: pytest is pinned there (the
# grading jobs need it at run time), so this file does not floor it - two declarations
# could only ever disagree.
-r requirements-autograde.txt
