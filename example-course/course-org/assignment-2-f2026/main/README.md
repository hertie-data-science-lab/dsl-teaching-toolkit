# Assignment 2 - normalise (individual, notebook)

In `starter.ipynb`, implement `solve(xs)` to min-max normalise the list to [0, 1].

Restart the kernel and run all cells before you commit, and commit the notebook with its outputs saved: we read it as it stands, and we run it the same way.
